require("dotenv").config();

const connectDB = async () => {
  // Placeholder for future MongoDB setup.
  console.log("[DB] Running in mock mode. MongoDB connection is intentionally disabled.");
};

module.exports = connectDB;
